<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="icandoallthingsthroughchrist10.blazeownerapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".ManageBoardingHouse" />
        <activity android:name=".SetUpBoardingHouse" />
        <activity android:name=".ManageGallery" />
        <activity android:name=".MapsProfileUpdateActivity" />
        <activity
            android:name=".BhouseProfileBotNav"
            android:label="@string/title_activity_bhouse_profile_bot_nav" />
        <activity android:name=".ManageReservationTickets" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <!-- <service android:name=".services.MyFirebaseMessagingService"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="com.google.firebase.MESSAGING_EVENT" /> -->
        <!-- </intent-filter> -->
        <!-- </service> -->

        <activity android:name=".AccountIsBlockOrPending" />
        <activity
            android:name=".tabbedActivity.ManageTenantsAccountsBotNav"
            android:label="@string/title_activity_manage_tenants_accounts_bot_nav" />
        <activity android:name=".TermsAndConditions"></activity>
    </application>

</manifest>